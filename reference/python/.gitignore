__pycache__/
*.pyc

# Spec catalog and schema copied in at build time (single source of truth is
# the repo's spec/ and schema/; this is a build artifact, like Java's target/).
phisql/_data/
# LICENSE copied from the repo root at build time (source of truth is ../../LICENSE).
/LICENSE
# Cached ANTLR tool jar fetched by scripts/generate_parser.sh. The generated
# parser under phisql/_generated/ IS committed; only the tool jar is ignored.
.antlr/
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.venv/
venv/
.tox/
